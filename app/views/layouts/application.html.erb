<!DOCTYPE html>
<html>
  <head>
    <title>Rails7 + Vite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
    <%= favicon_link_tag asset_path('favicon-32x32.png') %>

  </head>

  <body>
    <div class="flex flex-col h-screen">
      <%= render "shared/navbar" %>
      <%= render "shared/flash" %>
      <main class="container flex flex-col flex-1 px-5 mx-auto my-12">
        <%= yield %>

        <!-- test hello controller with stimulus -->
<div data-controller="hello">
  <input data-hello-target="name" type="text">/</input>
  <button data-action="click->hello#greet" class = "btn-primary" >Greet</button>
</div>



      </main>
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
